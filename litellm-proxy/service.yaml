apiVersion: v1
kind: Service
metadata:
  name: litellm-proxy
  namespace: solace-agent-mesh
  labels:
    app: litellm-proxy
spec:
  selector:
    app: litellm-proxy
  ports:
    - name: http
      port: 80
      targetPort: 4000
  type: ClusterIP
